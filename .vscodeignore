.vscode/
node_modules/
src/
server/src/
server/*.json
test/
views/test/

patch_app/
patch_gen_gui/
	# どちらも拡張機能には組み込まない独立ツール（開発者が手元で実行する。
	# src/docs/legacy-app-patch.md参照）。Rustのビルド成果物（target/。
	# patch_gen_guiはcargo tauri devのdebugビルドで6GB規模）を含め
	# VSIXに混入させないこと

**/*.cs
**/*.d.ts
**/*.map
**/*.ts
**/*.vue

.claude/
CLAUDE.md
.eslintrc.json
.gitignore
eslint.config.mts
tools/
tsconfig*.json
.vscode-test*
	# 統合テスト実行時の VSCode ユーザーデータ

**/bun.lock
**/*.tsbuildinfo
**/eslint.config.*
	# 同梱する server/node_modules/ の中にも入っているので ** で
