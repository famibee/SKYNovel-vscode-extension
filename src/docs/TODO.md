# BlueSNovel / SKYNovel拡張機能 TODO

最終更新：2026-08-26（完了分は CHANGELOG.md へ移動。最終的にカラを目指す）

**このファイルの使い方**：下の「残件一覧」だけ見れば、いま何ができるか分かる。
**このファイルは「いま何をすべきか」だけを持つ。** 設計判断の根拠と経緯・調査結果・実測値（測定値・踏んだ罠・やらないと決めた理由）・宿題の詳細は [README.md](README.md) 。

### ⚠️ 数字を書くかどうかの基準

**「状態」を書くと腐る。「出来事」を書けば腐らない。**

| 書く | 書かない |
|---|---|
| **期日・相手の都合**（8/23 以降、クールダウン30日） | **自分の成果物の現在値**（vsix が何 KB、いまの版番号） |
| **測定の記録**（2026-07-28 に測ったら 4.6ms）。日付とセットなら事実として固定される | **版ごとのサイズ一覧**。リリースのたびに更新が要る＝負債 |
| **観測した環境**（macOS / VSCode 1.130 で確認）。いつの話か分かる | **依存の版番号**。`package.json` に正があるものの写し |
| **「この版でないと動かない」という制約**（engines を満たさないフォークがある） | **完了の目印としての版番号**。CHANGELOG に正がある。**日付で足りる** |

判断に使った数字（「4.6ms しか縮まないからやらない」）は**根拠**なので残す。
現状報告の数字（「いま 2958KB」）は**次の変更で黙って嘘になる**ので書かない。
サイズを知りたければ `bun run release` が出す。

---

## 残件一覧

**決着した項目はこの一覧から行ごと消す**（経緯は各節に残す）。

### 期日あり

| # | 内容 | 時期 | 節 |
|---|---|---|---|
| 1 | **Marketplace 再申請**（新 ID `famibee2.bluesnovel`） | **是正版 v4.33.2 を公開済み・是正報告メール送信済み（2026-08-26）。Microsoft からの次の返信待ち** | §1 |
| 2 | **v5.0.0 正式版**＋**移行案内の公開**（README / ブログ / テンプレ README の3本） | **8/23 以降・再申請と同時** | [release.md](release.md) |

### 構造の作り直し【この順で。2026-07-30 に順序を決定】

⚠️ **順序に意味がある。** 下ほど上に依存する。**飛ばすと同じ場所を二度触る。**

| # | 内容 | なぜこの位置か | 節 |
|---|---|---|---|
| 1 | **マルチルートで static が後勝ち** | **最重**（別プロジェクトの設定で暗号化しかねない）だが**小さく独立**。パスにも他にも触らないので、先に出して単独で確かめられる | [file-watch.md](file-watch.md)(A) |
| 2 | **パス表現の整理**（`URI` オブジェクト化） | **根**。3 の一部はこれで消え、残りもここで入れる比較関数の上に乗る。**先に 3 をやると二度手間** | [build.md](build.md)（パス表現の整理） |
| 3 | **マルチルートの残り5件＋リソース解放5件** | 2 の上で。**済んだら `keywords` に `multi-root ready` を戻す**（2026-07-29 に外した） | [multiroot.md](multiroot.md) |
| 4 | 500ms のまとめが監視ごと | 1 と逆行するので 1 の後 | [file-watch.md](file-watch.md)(B) |
| 5 | `updPathJson()` が3役 | 分割 | [file-watch.md](file-watch.md)(C) |
| 6 | **再検証するファイルを絞る**（73.6ms・75%。天井は 1/8〜1/10） | 独立。絞り込みを誤ると**誤診断が残る／消えない**ので、落ち着いてから | [lsp-design.md](lsp-design.md) 宿題3 |

⚠️ **8/23 の再申請をまたがないこと。** 再申請では
**公開済みの版（GitHub Releases ＋ SHA256）を是正の証拠として示す**（§1）。
**2 は規模が大きい**ので、8/23 までに終わらないなら**着手を再申請後にずらす**。
中途半端な状態の版しか無い状況で再申請を迎えるのは避ける。
（1 は小さいので、先に出しておける）

### いつでも安全に着手できる

**現在なし。**

### 手を動かす必要がある／別環境が要る

| # | 内容 | 節 |
|---|---|---|
| 1 | **D&D 12ケースの計測**。mac 6 件は今すぐ可能。設計判断がここで止まっている | [file-watch.md](file-watch.md) |
| 2 | エディタ主導の変名で二重呼び出し。**Windows の挙動が未確認**なので直す前に検証 | [file-watch.md](file-watch.md)(D) |
| 3 | Windows 環境の自動テスト | [testing.md](testing.md) |

### 前提が揃うまで着手できない

| # | 内容 | 何を待っているか | 節 |
|---|---|---|---|
| 1 | ギャラリーのリンクをエンジンで切り替える | **BlueSNovel 版ギャラリーの頁が存在しない**（作者が作れば着手可） | [features.md](features.md) |
| 2 | tmpwiz を `contributes.walkthroughs` に載せる | **v5.0.0 の移行案内と一緒に出す話**（walkthrough はインストール時に自動で開くので、alpha 中に出すと落ち着いていない画面を全員に見せる） | [features.md](features.md) |

### ❓ 判断待ち（こちらから確認したいこと）

**現在なし。**

⚠️ **判断を仰いだことは、その場で言うだけでなく必ずここへ書く。**
`.claude/settings.json` の件は口頭で指摘しただけで一覧に入れず、
**そのままコミットされた**（2026-07-28。その後 `.gitignore` の `.claude/` で
追跡外に戻した）。会話の流れに出しただけでは残らない。

---

## 0. 受付箱

質問・要望・不具合を受けたらまずここに書き、処理したら下の該当節へ移す。**常にカラを目指す。**

- 段階的開発環境準備 (2026-08-26)
	現状、ゲームエンジン使用者にはnodejs(win環境ではPython)＆拡張機能インストールをまとめてやらせているが、アプリビルドの瞬間までnodejsがいらないので、ビルドボタンを押すまで遅延できないかというアイディア。つまり初見の試用時に【拡張機能インストールだけでいい】という導入の簡単さを提供したい
	ただしwin環境ではアイコン作成機能などでPythonを使用しているので、Python依存を排除できないと実行は出来ないと思われる。
	この辺を検討したい、というTODO。

### ✅ 決着した判断（経緯だけ残す）

#### ✅ UI に残る「SKYNovel」表示【決着・実装済み・2026-07-28】

**判断（2026-07-28）：`displayName` は両方名乗り、エディタ内の表示は BlueSNovel に寄せる。**

根拠は**利用者の状態**だった。再申請は 8/23 以降で、その頃の利用者は
**旧版をアンインストールして新版を入れる**という手順を踏んでいる。
つまり「新しい拡張機能だ」という意識がある状態で画面を見るので、
エディタ内で SKYNovel を名乗り続ける必要がない。

| 場所 | 決定 | なぜ |
|---|---|---|
| `displayName` | **`BlueSNovel / SKYNovel`** | Marketplace の**検索**で SKYNovel から到達させる必要がある |
| エディタ内の表示（ツールチップ・設定見出し・デバッガ名・`.ssn` エディタ名・category） | **`BlueSNovel`** | 入れ直した後に見る画面。短い方が読める |
| 識別子すべて | **変えない** | `skynovel.*` コマンドID・言語ID `skynovel`・文法スコープ `source.skynovel`・デバッガ型・`SKYNovel.score`。変えると利用者のキーバインド・設定・`launch.json`・ファイル関連付けが壊れる |
| エンジンを指していた名前 | **中立化** | 「Update SKYNovel」→「Update engine and template」／「SKYNovel GUIで開く」→「GUIで開く」 |

⚠️ **エンジンで切り替えられない**（この制約が判断を縛った）。v4.31.2 で
コマンドパレットの見出しを切り替えられたのは**コマンドを2つ登録できたから**
（`skynovel.openReferencePallet` と `bluesnovel.openReferencePallet`）。
`viewsContainers.title` などは**静的な文字列**で、VSCode 起動時に1つに決まる。

---

## 1. VS Marketplace 是正対応【詳細は非公開・`TODO.private.md` へ移動】

⚠️ **交渉の経緯（メール要約・時系列・判断根拠）等は `TODO.private.md`（git 管理外）へ**
このリポジトリは Public。削除の経緯や Microsoft とのやり取りの詳細は
公開しておく必要がないと判断したため（§0 参照）。

現在分かっている範囲の要約：

- コード側の是正（依存の整理・pip install の同意化）は完了済み（CHANGELOG 参照）
- 削除された旧拡張機能の ID は復活しないため、**新しい extension name
`famibee2.bluesnovel` で公開し直している**（[features.md](features.md)）
- 是正版 **v4.33.2** を GitHub Releases に公開済み：
https://github.com/famibee/SKYNovel-vscode-extension/releases/tag/v4.33.2
- Microsoft への是正報告メール（4通目への返信）は**送信済み（2026-08-26）**
- 現状は先方からの次の返信待ち
